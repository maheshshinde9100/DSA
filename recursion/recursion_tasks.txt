🟢 Beginner Level – Learn Recursion Basics
Factorial of a number
Input: 5 → Output: 120 //done

Fibonacci number
Input: 6 → Output: 8    //done

Sum of digits
Input: 1234 → Output: 10   //done

Reverse a number
Input: 1234 → Output: 4321  //done

Check if a string is a palindrome
Input: "madam" → Output: true     // done

Count number of zeros in a number
Input: 102030 → Output: 3     //done

Print numbers from N to 1 and 1 to N recursively
Input: 5 → Output: 5 4 3 2 1        //done

🟡 Intermediate Level – Recursion with Decisions
Power of a number (a^b)
Input: 2^5 → Output: 32

Find GCD (using Euclidean Algorithm recursively)
Input: 48, 18 → Output: 6

Find nth element of a custom sequence (like Tribonacci)
T(n) = T(n-1) + T(n-2) + T(n-3)

Check if array is sorted
Input: [1, 2, 3, 4] → Output: true

Linear Search recursively
Input: [1, 3, 5, 7], target = 5 → Output: index 2

Find first and last occurrence of a character in string
Input: "abacada", char = 'a' → Output: first=0, last=6

🔴 Advanced Level – Backtracking & Recursion Together
Permutations of a string
Input: "abc" → Output: abc, acb, bac, bca, cab, cba

All subsets of an array (Power Set)
Input: [1,2] → Output: [], [1], [2], [1,2]

Generate balanced parentheses
Input: n = 2 → Output: (()), ()()

Sudoku Solver
Use recursion + backtracking to fill in the puzzle.

Rat in a Maze / Path Finding
Move in grid: right and down to reach destination.

N-Queens Problem
Place N queens on an NxN board without attacking each other.

Word Break Problem (with Dictionary)
Break a string into dictionary-valid words using recursion.